---
title: "Quality gates"
kind: "reference"
audience: [contributor, maintainer, agent]
canonical_for: [quality_gate_reference]
requires: []
---

# Quality gates

This page will be the reference for the gate: the recipes `just check` runs in order, the
snapshot it takes of the worktree between them and what a changed file means, every hook
in the read only configuration with its pin, the paths every hook skips, the asset check
that proves its own checker before reading the manifest, and the two validators over the
handbook and the agent contract.

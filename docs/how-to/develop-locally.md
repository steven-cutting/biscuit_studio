---
title: "Develop locally"
kind: "how-to"
audience: [contributor, maintainer, agent]
canonical_for: [local_development]
requires: []
---

# Develop locally

This page will be the local setup procedure: the token carrying `read:packages` that
`npm ci` needs in the user's npm configuration to read the platform package from GitHub
Packages, Git LFS installed for this repository so the model's native sources arrive as
files rather than pointer text, the development server, the preview of a built site, and
what a secondary worktree must not do with the hooks.
